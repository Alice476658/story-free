{
  "name": "story-free-reader-pwa",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "opencc-js": "^1.0.5"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.30.1",
    "vite": "^7.0.0",
    "wrangler": "^4.77.0"
  }
}
